#asm
    include "../../../../../../../EndUser/sdcard/sdk/asm/inc/basic_stub.inc"
main:
    jp      _main
#endasm

char *b = 0x3000;

main() {
//    dinges();
//    a = (b + (2 * -a)) & 1;
//    a = (a && b) || 5;
//    a = dinges(1,2,3);
}
